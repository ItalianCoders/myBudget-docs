In order to run open api editor please follow these steps:
1)npm install -g http-server

2) unzip swagger-editor 
3)http-server swagger-editor

4) run on the browser the url http://localhost:8080
5) import the file rest-api-yaml